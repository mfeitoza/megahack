# Migration `20200703221118-three`

This migration has been generated by Marcus Feitoza at 7/3/2020, 10:11:18 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."Company" (
"address" TEXT NOT NULL  ,"city" TEXT NOT NULL  ,"company" TEXT NOT NULL  ,"id" TEXT NOT NULL  ,"state" TEXT NOT NULL  ,"userId" TEXT NOT NULL  ,"zipCode" TEXT NOT NULL  ,
    PRIMARY KEY ("id"),FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE)

CREATE TABLE "quaint"."new_User" (
"email" TEXT NOT NULL  ,"id" TEXT NOT NULL  ,"name" TEXT NOT NULL  ,"phone" TEXT NOT NULL  ,
    PRIMARY KEY ("id"))

INSERT INTO "quaint"."new_User" ("email", "id", "name") SELECT "email", "id", "name" FROM "quaint"."User"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."User";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_User" RENAME TO "User";

CREATE UNIQUE INDEX "quaint"."User.email" ON "User"("email")

CREATE TABLE "quaint"."new_Response" (
"companyId" TEXT NOT NULL  ,"createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,"description" TEXT NOT NULL  ,"id" TEXT NOT NULL  ,"title" TEXT NOT NULL  ,
    PRIMARY KEY ("id"),FOREIGN KEY ("companyId") REFERENCES "Company"("id") ON DELETE CASCADE ON UPDATE CASCADE)

INSERT INTO "quaint"."new_Response" ("createdAt", "description", "id", "title") SELECT "createdAt", "description", "id", "title" FROM "quaint"."Response"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."Response";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_Response" RENAME TO "Response";

CREATE TABLE "quaint"."new_Request" (
"companyId" TEXT NOT NULL  ,"createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,"description" TEXT NOT NULL  ,"id" TEXT NOT NULL  ,"title" TEXT NOT NULL  ,"validUntil" DATE NOT NULL  ,
    PRIMARY KEY ("id"),FOREIGN KEY ("companyId") REFERENCES "Company"("id") ON DELETE CASCADE ON UPDATE CASCADE)

INSERT INTO "quaint"."new_Request" ("createdAt", "description", "id", "title", "validUntil") SELECT "createdAt", "description", "id", "title", "validUntil" FROM "quaint"."Request"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."Request";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_Request" RENAME TO "Request";

CREATE UNIQUE INDEX "quaint"."Company_userId" ON "Company"("userId")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200703114258-two..20200703221118-three
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource DS {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
@@ -11,20 +11,28 @@
 // Define your own datamodels here and run `yarn redwood db save` to create
 // migrations for them.
 // TODO: Please remove the following example:
 model User {
-  id        String     @id @default(cuid())
-  email     String     @unique
-  name      String
-  company   String
-  zipCode   String
-  address   String
-  state     String
-  city      String
-  requests  Request[]
-  responses Response[]
+  id      String   @id @default(cuid())
+  email   String   @unique
+  name    String
+  phone   String
+  company Company?
 }
+model Company {
+  id       String     @id @default(cuid())
+  company  String
+  zipCode  String
+  address  String
+  state    String
+  city     String
+  user     User       @relation(fields: [userId], references: [id])
+  userId   String
+  Request  Request[]
+  Response Response[]
+}
+
 model Tag {
   id       Int       @id @default(autoincrement())
   name     String    @unique
   requests Request[] @relation(references: [id])
@@ -35,10 +43,10 @@
   title             String
   description       String
   tags              Tag[]               @relation(references: [id])
   createdAt         DateTime            @default(now())
-  createdBy         User                @relation(fields: [createdById], references: [id])
-  createdById       String
+  company           Company             @relation(fields: [companyId], references: [id])
+  companyId         String
   validUntil        DateTime
   ReponsesToRequest ReponsesToRequest[]
 }
@@ -46,10 +54,10 @@
   id                String              @id @default(cuid())
   title             String
   description       String
   createdAt         DateTime            @default(now())
-  created           User                @relation(fields: [createdBy], references: [id])
-  createdBy         String
+  company           Company             @relation(fields: [companyId], references: [id])
+  companyId         String
   ReponsesToRequest ReponsesToRequest[]
 }
 model ReponsesToRequest {
```


